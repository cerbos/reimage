---
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

ignore:
  - "tools"

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: true
